@model IEnumerable<DistriHub.Models.ProductDetails>
@{
    ViewData["Title"] = "Product Details";
    var serial = (string?)ViewData["Serial"] ?? string.Empty;
}
@section Styles {
    <link rel="stylesheet" href="~/css/serial.css" />
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" />
    <link rel="stylesheet" href="https://cdn.datatables.net/buttons/2.4.1/css/buttons.dataTables.min.css" />
    <style>
        /* Responsive table: stack cells on small screens using data-label */
        @@media (max-width: 767.98px) {
            .table-responsive table thead { display: none; }
            .table-responsive table, .table-responsive tbody, .table-responsive tr, .table-responsive td {
                display: block;
                width: 100%;
            }
            .table-responsive tr { margin-bottom: 1rem; border: 1px solid #e9ecef; padding: .5rem; }
            .table-responsive td {
                text-align: left;
                padding-left: 50%;
                position: relative;
                white-space: normal;
            }
            .table-responsive td::before {
                content: attr(data-label);
                position: absolute;
                left: .75rem;
                top: .5rem;
                font-weight: 600;
                width: 45%;
                white-space: nowrap;
                overflow: hidden;
                text-overflow: ellipsis;
            }
        }
    </style>
}

<section class="tab-components">
    <div class="container-fluid">
        <div class="title-wrapper pt-30">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <div class="title">
                        <h2>Product Details</h2>
                    </div>
                </div>
            </div>
        </div>

        <div class="form-elements-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card-style mb-30">
                        <div class="table-responsive">
                            <div class="mb-3 d-flex align-items-center">
                                @* <label for="serialFilter" class="me-2 mb-0">Serial:</label>
                                <input id="serialFilter" class="form-control form-control-sm me-2" style="max-width:300px;" value="@ViewData["Serial"]" />
                                <button id="btnFilter" class="btn btn-sm btn-primary">Filter</button>
                                <button id="btnClear" class="btn btn-sm btn-secondary ms-2">Clear</button> *@
                            </div>
                            <table id="productTable" class="table table-hover align-middle table-striped" style="width:100%">
                                <thead class="table-light">
                                    <tr class="text-center">
                                        @* <th scope="col">ProductId</th> *@
                                        <th scope="col">Serial No</th>
                                        <th scope="col">Category Name</th>
                                        <th scope="col">Sub Category Name</th>
                                        <th scope="col">Model Name</th>
                                        @* <th scope="col">Upload Date</th> *@
                                        <th scope="col">Is Used Display</th>
                                        <th scope="col">Distributor</th>
                                        <th scope="col">Dealer</th>
                                        <th scope="col">Finance</th>
                                        <th scope="col">FinanceDate</th>
                                        <th scope="col">Installation</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @* DataTable will populate rows via AJAX *@
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" crossorigin="anonymous"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <!-- JSZip for excelHtml5 export. Use a reliable CDN without integrity to avoid mismatched hash errors in some environments -->
    <script src="https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/dataTables.buttons.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.4.1/js/buttons.html5.min.js"></script>
    <script>
        $(document).ready(function () {
            var table = $('#productTable').DataTable({
                // show length menu, buttons, filter, table
                dom: 'lBfrtip',
                buttons: [
                    {
                        extend: 'excelHtml5',
                        text: 'Export to Excel',
                        titleAttr: 'Export to Excel',
                        filename: 'ProductDetails',
                        exportOptions: {
                            columns: ':visible',
                            modifier: {
                                // When serverSide is true, export will only include rows currently loaded on client.
                                // Set to 'all' if you want to export unfiltered server data (requires server support).
                                search: 'applied',
                                order: 'applied'
                            }
                        }
                    }
                ],
                responsive: true,
                processing: true,
                serverSide: true, // server-side processing for large datasets
                deferRender: true,
                paging: true,
                // default to showing All entries (DataTables uses -1 for 'All')
                pageLength: 10,
                lengthMenu: [[10, 25, 50, -1], [10, 25, 50, 'All']],
                language: {
                    lengthMenu: 'Show _MENU_ entries'
                },
                searching: true,
                ordering: true,
                ajax: {
                    url: '/SerialNoView/ApiGetProductDetails',
                    type: 'POST', // use POST to avoid very long query strings from DataTables
                    dataSrc: 'data',
                    data: function (d) {
                        // send serial filter explicitly if present
                        d.serial = $('#serialFilter').val();
                    }
                },
                columns: [
                    { data: 'serialNo' },
                    { data: 'categoryName' },
                    { data: 'subCategoryName' },
                    { data: 'modelName' },
                    // { data: 'uploadDate', className: 'text-center', render: function (data) { return data ? data.substring(0, 10) : ''; } },
                    { data: 'isUsedDisplay' },
                    { data: 'distributor' },
                    { data: 'dealer' },
                    { data: 'finance' },
                    { data: 'financeDate', render: function (data) { return data ? data.substring(0, 10) : ''; } },
                    { data: 'installation' }
                ],
                columnDefs: [
                    { targets: [1,2,3], className: 'text-center' }
                ]
            });
        });
    </script>
}
